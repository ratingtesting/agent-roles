name: web-crawl4ai
version: 1.0.0
description: "Crawl4AI web content extraction — free, local, no API keys. Install with `pip install crawl4ai` and `playwright install chromium`."
author: user
kind: backend
provides_web_providers:
  - crawl4ai